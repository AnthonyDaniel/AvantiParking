import { ParkingLotPipePipe } from './parking-lot-pipe.pipe';

describe('ParkingLotPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingLotPipePipe();
    expect(pipe).toBeTruthy();
  });
});
