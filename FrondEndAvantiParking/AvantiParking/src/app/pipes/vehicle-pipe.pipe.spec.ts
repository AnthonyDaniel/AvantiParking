import { VehiclePipePipe } from './vehicle-pipe.pipe';

describe('VehiclePipePipe', () => {
  it('create an instance', () => {
    const pipe = new VehiclePipePipe();
    expect(pipe).toBeTruthy();
  });
});
