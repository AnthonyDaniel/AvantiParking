import { HeadquarterPipePipe } from './headquarter-pipe.pipe';

describe('HeadquarterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HeadquarterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
